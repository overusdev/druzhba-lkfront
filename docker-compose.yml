version: "3.3"
services:
  frontendlk:
    build: .
    image: lk.druzba-nn.site
    container_name: lk.druzba-nn.site
    # env_file:
    #   - .env
    ports:
      - 8000:8000
    restart: always
    volumes:
      - .:/var/www/lk.druzba-nn.site
      - "/var/www/lk.druzba-nn.site/node_modules"